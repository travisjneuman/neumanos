/**
 * Synapse Module - Neural Search Interface
 *
 * Exports Synapse (the neural search component) and related utilities.
 * Named after synapses - connection points in the brain where signals fire rapidly.
 */

export { CommandPalette } from './CommandPalette';
export { default } from './CommandPalette';
export * from './types';
export * from './searchRegistry';
