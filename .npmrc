# Fix Rollup platform-specific optional dependencies for cross-platform builds
# See: https://github.com/npm/cli/issues/4828
# This ensures optional dependencies (like @rollup/rollup-linux-x64-gnu) are
# properly installed in CI/CD environments regardless of dev machine platform.
optional=true
